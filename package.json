{
  "name": "subject-code-gen",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "cd server && yarn install && cd ../client && yarn install && yarn build",
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Kunwar-Aditya-Codes/subject-code-generator.git"
  }
}
